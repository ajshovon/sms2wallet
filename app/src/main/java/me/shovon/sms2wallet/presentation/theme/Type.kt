package me.shovon.sms2wallet.presentation.theme

import androidx.compose.material3.Typography

/** Default Material 3 type scale. No custom font is bundled, so this is intentionally the
 * stock scale - kept as its own file so a custom [androidx.compose.ui.text.font.FontFamily]
 * can be dropped in later without touching [Theme.kt]. */
val AppTypography = Typography()
